# Unknown from repository scan

The repository scan did not provide architecture.md. To complete this document, inspect:
- Presence and contents of architecture diagrams or description
- Major modules: azuresite (production and settings), polls (models, views), templates, and static files
- Data flow: how HTTP requests traverse URLs to views to models and templates
- Any design tradeoffs related to deployment on Azure App Service and PostgreSQL
